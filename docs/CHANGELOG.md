# Changelog

## [Unreleased]

### Added
- Added Health Check API endpoint: `GET /api/v1/health`.
- Added `HealthController` for application health status.
- Added `HealthResponse` DTO for standardized health response.

### Fixed
- Corrected the `HealthResponse` package name from `resonse` to `response`.